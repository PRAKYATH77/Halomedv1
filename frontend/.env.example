# Copy this file to `.env` in the `frontend/` folder and fill in real values.
# Vite requires public environment variables to start with `VITE_`.

# Where frontend will call your backend API
VITE_API_BASE_URL=http://localhost:5003

# Razorpay key ID (public) — use the Test API Key ID here (starts with rzp_test_...)
VITE_RAZORPAY_KEY_ID=rzp_test_Ss3CXRBF36u7Yi
