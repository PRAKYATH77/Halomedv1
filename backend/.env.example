# Copy this file to `.env` in the `backend/` folder and fill in real values.
# Do NOT commit your real `.env` to git. The `backend/.gitignore` already ignores `.env`.
#
# Development example values (replace placeholders with your test keys):
PORT=5003
# Database connection (example): mysql://user:pass@localhost:3306/halomed
DATABASE_URL=

# JWT secret for auth tokens
JWT_SECRET=change_this_to_a_long_random_value

# Razorpay test keys (server-side only - keep the secret private)
RAZORPAY_KEY_ID=rzp_test_Ss3CXRBF36u7Yi
RAZORPAY_KEY_SECRET=k2VZtBVPPf5DNyEQFRFdr84z

# Optional: webhook secret for verifying Razorpay webhooks
RAZORPAY_WEBHOOK_SECRET=optional_webhook_secret_here

NODE_ENV=development
PORT=5000
NODE_ENV=development

# Database Configuration
DB_HOST=localhost
DB_USER=halomed_user
DB_PASSWORD=halomed_pass123
DB_NAME=halomed_pharmacy
DB_PORT=3306

# JWT Configuration
JWT_SECRET=your_jwt_secret_key_change_in_production
JWT_EXPIRE=7d

# ML Module
ML_API_URL=http://localhost:5001
